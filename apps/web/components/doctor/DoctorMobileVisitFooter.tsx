export type SaveStatus = "Saving" | "Saved" | "Offline draft" | "Sync failed";
export function DoctorMobileVisitFooter({ status, onAction }: { status: SaveStatus; onAction(action: "save" | "rx" | "requests" | "finish"): void }) { return <footer className="doctor-mobile-visit-footer"><output data-save-status={status}>{status}</output><div><button onClick={() => onAction("save")} type="button">Save Draft</button><button onClick={() => onAction("rx")} type="button">Rx</button><button onClick={() => onAction("requests")} type="button">Requests</button><button onClick={() => onAction("finish")} type="button">Finish</button></div></footer>; }
