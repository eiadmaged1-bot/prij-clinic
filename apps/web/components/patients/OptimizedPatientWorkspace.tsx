import type { ReactNode } from "react";
export function OptimizedPatientWorkspace({ children }: { children: ReactNode }) { return <div className="patient-workspace optimized-patient-workspace">{children}</div>; }
