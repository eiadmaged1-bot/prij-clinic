import assert from "node:assert/strict";
import fs from "node:fs";
if(!process.env.TEST_WEB_ORIGIN||new URL(process.env.TEST_WEB_ORIGIN).port!=="3100"||!process.env.TEST_SESSION_TOKEN||!process.env.TEST_CSRF_TOKEN)throw new Error("Explicit isolated web/session variables are required.");const origin=process.env.TEST_WEB_ORIGIN,cookie=`prij_clinic_session=${process.env.TEST_SESSION_TOKEN}; csrf-token=${process.env.TEST_CSRF_TOKEN}`;const me=await fetch(`${origin}/api/backend/auth/me`,{headers:{cookie}});assert.equal(me.status,200);assert.ok((await me.json()).user);const controller=fs.readFileSync("apps/api/src/auth/auth.controller.ts","utf8");for(const policy of ["httpOnly","sameSite","secure"])assert.match(controller,new RegExp(policy,"i"));console.log("Server-side session authentication and secure cookie policy passed without exposing a raw token payload.");
