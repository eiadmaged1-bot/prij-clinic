# Prij Clinic environment example
# Placeholder values only. Do not commit real secrets, passwords, tokens, or patient data.

API_PORT=3001
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DATABASE
JWT_SECRET=replace-with-local-development-secret
JWT_EXPIRES_IN=1h
DEMO_OWNER_EMAIL=owner@example.test
DEMO_OWNER_PASSWORD=replace-with-local-demo-password
SEED_DEMO_OWNER=false
