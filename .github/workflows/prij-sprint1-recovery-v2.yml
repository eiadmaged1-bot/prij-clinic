name: Prij Sprint 1 Recovery V2

on:
  workflow_dispatch:
  push:
    branches:
      - security/rbac-scope-enforcement
    paths:
      - .github/workflows/prij-sprint1-recovery-v2.yml

permissions:
  contents: write

concurrency:
  group: prij-sprint1-recovery-v2
  cancel-in-progress: false

jobs:
  recover:
    name: Backup, verify Feature 46, and open QA repair batch
    runs-on: [self-hosted, Windows, X64]
    timeout-minutes: 180

    steps:
      - id: prepare
        name: Prepare isolated SECTRA workspace
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          if ($env:RUNNER_NAME -ne 'SECTRA') { throw "Expected SECTRA, got '$env:RUNNER_NAME'." }
          if ($env:RUNNER_OS -ne 'Windows') { throw "Expected Windows, got '$env:RUNNER_OS'." }
          foreach ($name in @('control-v2', 'workspace-v2', 'diagnostics-v2')) {
            $target = Join-Path $env:GITHUB_WORKSPACE $name
            if (Test-Path -LiteralPath $target) { Remove-Item -LiteralPath $target -Recurse -Force }
          }
          New-Item -ItemType Directory -Force (Join-Path $env:GITHUB_WORKSPACE 'diagnostics-v2') | Out-Null

      - name: Checkout recovery controls
        uses: actions/checkout@v4
        with:
          ref: docs/200-feature-architecture-and-sprint-a
          fetch-depth: 0
          clean: true
          path: control-v2

      - name: Checkout protected Sprint 1 baseline
        uses: actions/checkout@v4
        with:
          ref: work/sprint1-fix3-protected
          fetch-depth: 0
          clean: true
          path: workspace-v2

      - id: backup
        name: Create and verify local-only PostgreSQL backup
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          $projectRoot = 'C:\Newfolder\prij-clinic'
          $envPath = Join-Path $projectRoot '.env'
          if (-not (Test-Path -LiteralPath $envPath)) { throw "Missing local environment file: $envPath" }

          $databaseUrl = $null
          foreach ($line in Get-Content -LiteralPath $envPath) {
            $trimmed = $line.Trim()
            if (-not $trimmed -or $trimmed.StartsWith('#') -or -not $trimmed.Contains('=')) { continue }
            $parts = $trimmed.Split('=', 2)
            if ($parts[0].Trim() -eq 'DATABASE_URL') {
              $databaseUrl = $parts[1].Trim().Trim('"').Trim("'")
              break
            }
          }
          if (-not $databaseUrl) { throw 'DATABASE_URL is missing from the local Prij .env.' }

          $uri = [System.Uri]$databaseUrl
          $userInfo = $uri.UserInfo.Split(':', 2)
          $dbUser = [System.Uri]::UnescapeDataString($userInfo[0])
          $dbPassword = if ($userInfo.Count -gt 1) { [System.Uri]::UnescapeDataString($userInfo[1]) } else { '' }
          $dbName = [System.Uri]::UnescapeDataString($uri.AbsolutePath.TrimStart('/'))
          $timestamp = Get-Date -Format 'yyyyMMdd-HHmmss'
          $backupDir = Join-Path $projectRoot "backups\sprint1-recovery-v2-$timestamp"
          New-Item -ItemType Directory -Force $backupDir | Out-Null
          $dumpPath = Join-Path $backupDir 'prij-clinic-pre-repair.dump'
          $manifestPath = Join-Path $backupDir 'BACKUP-MANIFEST.txt'
          $diag = Join-Path $env:GITHUB_WORKSPACE 'diagnostics-v2'

          $docker = Get-Command docker.exe -ErrorAction SilentlyContinue
          if (-not $docker) { throw 'docker.exe was not found on SECTRA.' }
          $dockerPath = $docker.Source
          $container = 'prij-clinic-postgres'
          $previousPreference = $ErrorActionPreference
          $ErrorActionPreference = 'Continue'
          try {
            & $dockerPath inspect $container *> (Join-Path $diag 'docker-inspect.log')
            $inspectExit = $LASTEXITCODE
            if ($inspectExit -ne 0) { throw "PostgreSQL container '$container' is unavailable. Exit $inspectExit" }

            $remotePath = "/tmp/prij-clinic-pre-repair-$timestamp.dump"
            & $dockerPath exec --env "PGPASSWORD=$dbPassword" $container pg_dump --host 127.0.0.1 --port 5432 --username $dbUser --format custom --no-owner --no-privileges --file $remotePath $dbName *> (Join-Path $diag 'pg-dump.log')
            $dumpExit = $LASTEXITCODE
            if ($dumpExit -ne 0) { throw "Docker pg_dump failed with exit code $dumpExit." }

            & $dockerPath cp "$container`:$remotePath" $dumpPath *> (Join-Path $diag 'docker-copy.log')
            $copyExit = $LASTEXITCODE
            if ($copyExit -ne 0) { throw "docker cp failed with exit code $copyExit." }
            & $dockerPath exec $container rm -f $remotePath *> $null
          } finally {
            $ErrorActionPreference = $previousPreference
          }

          if (-not (Test-Path -LiteralPath $dumpPath)) { throw 'Database dump file was not created.' }
          $dump = Get-Item -LiteralPath $dumpPath
          if ($dump.Length -le 0) { throw 'Database dump is empty.' }
          $hash = (Get-FileHash -LiteralPath $dumpPath -Algorithm SHA256).Hash
          @(
            'Prij Clinic Sprint 1 Recovery V2 backup',
            "Created: $(Get-Date -Format 'yyyy-MM-dd HH:mm:ss K')",
            'Method: docker-pg_dump',
            "Path: $dumpPath",
            "Size bytes: $($dump.Length)",
            "SHA256: $hash",
            'Uploaded to GitHub: NO',
            'Database mutation performed: NO'
          ) | Set-Content -LiteralPath $manifestPath -Encoding utf8
          "PRIJ_BACKUP_DIR=$backupDir" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          "PRIJ_BACKUP_SIZE=$($dump.Length)" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          "PRIJ_BACKUP_SHA=$hash" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - id: receipt
        name: Record backup receipt immediately
        working-directory: control-v2
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          git pull --rebase origin docs/200-feature-architecture-and-sprint-a
          New-Item -ItemType Directory -Force 'docs/verification' | Out-Null
          @(
            '# Sprint 1 Recovery V2 Backup Receipt',
            '',
            "- Created: $(Get-Date -Format 'yyyy-MM-dd HH:mm:ss K')",
            '- Runner: `SECTRA`',
            '- Method: `docker-pg_dump`',
            "- Local directory: `$env:PRIJ_BACKUP_DIR`",
            "- Size: $env:PRIJ_BACKUP_SIZE bytes",
            "- SHA256: `$env:PRIJ_BACKUP_SHA`",
            '- Uploaded to GitHub: **NO**',
            '- Database mutation performed: **NO**'
          ) | Set-Content -LiteralPath 'docs/verification/SPRINT1_RECOVERY_V2_BACKUP_RECEIPT.md' -Encoding utf8
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -- 'docs/verification/SPRINT1_RECOVERY_V2_BACKUP_RECEIPT.md'
          git commit -m 'docs: record Sprint 1 Recovery V2 backup'
          if ($LASTEXITCODE -ne 0) { throw 'Could not commit backup receipt.' }
          git push origin HEAD:docs/200-feature-architecture-and-sprint-a
          if ($LASTEXITCODE -ne 0) { throw 'Could not push backup receipt.' }

      - id: integrate
        name: Deterministically integrate Feature 46
        working-directory: workspace-v2
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          if (git status --porcelain) { throw 'Protected baseline is not clean.' }
          git fetch origin test/feature-46-refractory-complaint
          if ($LASTEXITCODE -ne 0) { throw 'Could not fetch Feature 46 branch.' }
          git checkout -B integration/sprint1-fix3-feature46
          if ($LASTEXITCODE -ne 0) { throw 'Could not create integration branch.' }

          $previousPreference = $ErrorActionPreference
          $ErrorActionPreference = 'Continue'
          try {
            git cherry-pick --no-commit 149c22dd7f3d91c82d184d7d4087403feb8506b3
            $applyExit = $LASTEXITCODE
          } finally {
            $ErrorActionPreference = $previousPreference
          }
          $unmerged = @(git ls-files -u)
          if ($applyExit -ne 0 -and $unmerged.Count -eq 0) { throw "Feature 46 failed without resolvable conflicts. Exit $applyExit" }
          if ($unmerged.Count -gt 0) {
            node ..\control-v2\scripts\resolve-sprint1-feature46-conflicts.mjs $env:GITHUB_WORKSPACE\workspace-v2
            if ($LASTEXITCODE -ne 0) { throw 'Deterministic conflict resolver failed.' }
          }
          git add --all
          if (git ls-files -u) { throw 'Unmerged index entries remain.' }
          if (git grep -n -E '^(<<<<<<<|=======|>>>>>>>)' -- ':!package-lock.json') { throw 'Conflict markers remain.' }
          git rev-parse -q --verify CHERRY_PICK_HEAD *> $null
          if ($LASTEXITCODE -eq 0) { git cherry-pick --quit }
          git diff --cached --check
          if ($LASTEXITCODE -ne 0) { throw 'Integrated changes have diff-check errors.' }

      - id: env
        name: Load local development configuration safely
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          $envPath = 'C:\Newfolder\prij-clinic\.env'
          $allowed = @('DATABASE_URL', 'JWT_SECRET', 'JWT_EXPIRES_IN', 'AI_FEATURES_ENABLED', 'AI_PROVIDER')
          $databaseFound = $false
          foreach ($line in Get-Content -LiteralPath $envPath) {
            $trimmed = $line.Trim()
            if (-not $trimmed -or $trimmed.StartsWith('#') -or -not $trimmed.Contains('=')) { continue }
            $parts = $trimmed.Split('=', 2)
            $key = $parts[0].Trim()
            if ($allowed -notcontains $key) { continue }
            $value = $parts[1].Trim().Trim('"').Trim("'")
            if ($key -eq 'DATABASE_URL' -and $value) { $databaseFound = $true }
            "$key=$value" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          }
          if (-not $databaseFound) { throw 'DATABASE_URL was not loaded.' }

      - id: install
        name: Install dependencies and generate Prisma Client
        working-directory: workspace-v2
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          npm ci
          if ($LASTEXITCODE -ne 0) { throw 'npm ci failed.' }
          npm run prisma:generate
          if ($LASTEXITCODE -ne 0) { throw 'Prisma Client generation failed.' }

      - id: checks
        name: Run all six mandatory checks
        working-directory: workspace-v2
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          $diag = Join-Path $env:GITHUB_WORKSPACE 'diagnostics-v2'
          $checks = [ordered]@{
            'clinical-workflow' = 'node scripts/clinical-workflow-unification-test.mjs'
            'fix3-medication' = 'node scripts/recovery-fix3-medication-test.mjs'
            'fix3-search' = 'node scripts/recovery-fix3-search-test.mjs'
            'feature46' = 'node scripts/feature-46-refractory-complaint-test.mjs'
            'typecheck' = 'npm run typecheck'
            'build' = 'npm run build'
          }
          $failures = @()
          foreach ($entry in $checks.GetEnumerator()) {
            $stdout = Join-Path $diag "$($entry.Key).stdout.log"
            $stderr = Join-Path $diag "$($entry.Key).stderr.log"
            $process = Start-Process -FilePath 'cmd.exe' -ArgumentList @('/d', '/s', '/c', $entry.Value) -NoNewWindow -Wait -PassThru -RedirectStandardOutput $stdout -RedirectStandardError $stderr
            if (Test-Path -LiteralPath $stdout) { Get-Content -LiteralPath $stdout | ForEach-Object { Write-Host $_ } }
            if (Test-Path -LiteralPath $stderr) { Get-Content -LiteralPath $stderr | ForEach-Object { Write-Host $_ } }
            if ($process.ExitCode -ne 0) { $failures += "$($entry.Key):$($process.ExitCode)" }
          }
          if ($failures.Count -gt 0) { throw "Mandatory checks failed: $($failures -join ', ')" }

      - id: push
        name: Commit and push verified integration
        working-directory: workspace-v2
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          New-Item -ItemType Directory -Force 'docs/verification' | Out-Null
          @(
            '# Sprint 1 Fix 3 + Feature 46 Integration Verification',
            '',
            '- Integration branch: `integration/sprint1-fix3-feature46`',
            '- Conflict resolution: deterministic',
            '- Prisma Client generation: PASS',
            '- Clinical workflow test: PASS',
            '- Fix 3 medication test: PASS',
            '- Fix 3 search test: PASS',
            '- Feature 46 test: PASS',
            '- Typecheck: PASS',
            '- Production build: PASS',
            "- Backup SHA256: `$env:PRIJ_BACKUP_SHA`",
            '- Database mutation: NONE'
          ) | Set-Content -LiteralPath 'docs/verification/SPRINT1_FIX3_FEATURE46_INTEGRATION.md' -Encoding utf8
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add --all
          git diff --cached --check
          git commit -m 'Integrate Sprint 1 Fix 3 with verified Feature 46'
          if ($LASTEXITCODE -ne 0) { throw 'Could not commit verified integration.' }
          git push origin HEAD:integration/sprint1-fix3-feature46
          if ($LASTEXITCODE -ne 0) { throw 'Could not push verified integration branch.' }

      - id: qa
        name: Create QA repair batch branch
        working-directory: workspace-v2
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          git checkout -B work/sprint1-qa-repair-batch
          @(
            '# Sprint 1 QA Repair Batch',
            '',
            'Baseline: `integration/sprint1-fix3-feature46`',
            '',
            '1. Pregnancy versus menstrual context separation.',
            '2. Manual and calculated EDD workflow.',
            '3. Compact calendars for high-resolution displays.',
            '4. Pregnancy history and EDD visual cleanup.',
            '5. Compact medication cards with prominent trade names.',
            '6. Structured Arabic prescription quantity, timing, duration, and Note controls.',
            '',
            'Status: STARTED — implementation pending.'
          ) | Set-Content -LiteralPath 'docs/verification/SPRINT1_QA_REPAIR_BATCH.md' -Encoding utf8
          git add -- 'docs/verification/SPRINT1_QA_REPAIR_BATCH.md'
          git commit -m 'Start Sprint 1 QA repair batch'
          if ($LASTEXITCODE -ne 0) { throw 'Could not commit QA repair checkpoint.' }
          git push origin HEAD:work/sprint1-qa-repair-batch
          if ($LASTEXITCODE -ne 0) { throw 'Could not push QA repair branch.' }

      - name: Record exact failure state
        if: failure()
        working-directory: control-v2
        shell: powershell
        env:
          PREPARE_OUTCOME: ${{ steps.prepare.outcome }}
          BACKUP_OUTCOME: ${{ steps.backup.outcome }}
          RECEIPT_OUTCOME: ${{ steps.receipt.outcome }}
          INTEGRATE_OUTCOME: ${{ steps.integrate.outcome }}
          ENV_OUTCOME: ${{ steps.env.outcome }}
          INSTALL_OUTCOME: ${{ steps.install.outcome }}
          CHECKS_OUTCOME: ${{ steps.checks.outcome }}
          PUSH_OUTCOME: ${{ steps.push.outcome }}
          QA_OUTCOME: ${{ steps.qa.outcome }}
        run: |
          $ErrorActionPreference = 'Stop'
          git pull --rebase origin docs/200-feature-architecture-and-sprint-a
          New-Item -ItemType Directory -Force 'docs/verification' | Out-Null
          $lines = @(
            '# Latest Sprint 1 Recovery V2 Failure',
            '',
            "- Run ID: $env:GITHUB_RUN_ID",
            "- Run URL: $env:GITHUB_SERVER_URL/$env:GITHUB_REPOSITORY/actions/runs/$env:GITHUB_RUN_ID",
            "- Prepare: $env:PREPARE_OUTCOME",
            "- Backup: $env:BACKUP_OUTCOME",
            "- Receipt: $env:RECEIPT_OUTCOME",
            "- Integrate: $env:INTEGRATE_OUTCOME",
            "- Environment: $env:ENV_OUTCOME",
            "- Install/Prisma: $env:INSTALL_OUTCOME",
            "- Six checks: $env:CHECKS_OUTCOME",
            "- Integration push: $env:PUSH_OUTCOME",
            "- QA branch: $env:QA_OUTCOME",
            '',
            '## Diagnostic tails'
          )
          $diag = Join-Path $env:GITHUB_WORKSPACE 'diagnostics-v2'
          if (Test-Path -LiteralPath $diag) {
            foreach ($file in Get-ChildItem -LiteralPath $diag -File | Sort-Object Name) {
              $lines += ''
              $lines += "### $($file.Name)"
              $lines += '```text'
              $lines += @(Get-Content -LiteralPath $file.FullName -Tail 100)
              $lines += '```'
            }
          }
          $report = 'docs/verification/LATEST_SPRINT1_RECOVERY_V2_FAILURE.md'
          $lines | Set-Content -LiteralPath $report -Encoding utf8
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -- $report
          git commit -m "ci: record Sprint 1 Recovery V2 failure $env:GITHUB_RUN_ID"
          if ($LASTEXITCODE -eq 0) { git push origin HEAD:docs/200-feature-architecture-and-sprint-a }
