# Drug Market Retail Metadata Policy

Retail public metadata connector is seeded disabled by default.

Rules:
- It cannot override official registry data.
- It cannot collect branch availability or purchase paths.
- It cannot use protected, login-only, CAPTCHA, cart, checkout, or stock endpoints.
- It may be enabled later only for product metadata after explicit owner/admin approval.
