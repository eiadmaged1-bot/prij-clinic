# Global Smart Search

Global smart search is available from the shared top bar and supports `Ctrl+K`.

Search remains role-aware and covers patients, phone, MRN, appointments, invoices, investigations, prescriptions, document metadata, medication/reference items, and guideline titles/chunks where permissions allow.

Normal UI avoids raw internal IDs unless shown as patient ID/MRN.
