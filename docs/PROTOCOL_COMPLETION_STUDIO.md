# Protocol Completion Studio

The existing protocol model and versioning workflow are reused. Completion questionnaires store section answers, connections, percentage, version, unresolved items, review state, and audit attribution. Blank sections stay blank; the UI explicitly instructs reviewers not to invent medical content. Applying a protocol records context only and does not automatically order, prescribe, or modify signed records.
