# QR workflow

Payloads contain only `PRIJ-PATIENT:<opaque UUID>`. HTTPS prefers rear camera/native `BarcodeDetector`, then lazily loads `jsqr`; preview and explicit check-in are mandatory. LAN HTTP disables camera. Real-device/ngrok scanning was not performed.
