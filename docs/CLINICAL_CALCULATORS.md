# Clinical calculators

Medication-linked calculators are collapsed by default. Execution requires an active approved version, named reviewer, declared inputs/ranges/units, source, population, exclusions, rounding rule, limitations, and passing locked test cases. Stored code is never evaluated. Missing or impossible values are rejected, stale observations warn, pre-rounding and final values are shown, and no result inserts a prescription automatically.
